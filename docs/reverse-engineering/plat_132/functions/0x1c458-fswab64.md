# 0x1c458 __fswab64

## Status

- Status: complete
- Confidence: verified single ARM64 `REV X0,X0`, unsigned 64-bit ABI, return,
  and both direct callers.
- Size: `0x8` bytes, 2 ARM64 instructions.
- Recovered signature: `u64 __fswab64(u64 value)`.

## Semantics

Returns the input with its eight bytes in reverse order.

## Caller Context

`dump_net_condition_set @ 0xb2ec` has two direct call sites that use this helper
while formatting network-condition fields.

## Concurrency and Ownership

Pure register-only transformation with no shared state or ownership effects.

## Evidence

- Complete ARM64 body: `REV X0,X0; RET`.
- Exhaustive direct xref query found two call sites in `dump_net_condition_set`.
- IDA type at `0x1c458` updated to the recovered unsigned 64-bit signature.

## Source-Like Reconstruction

The semantic C reconstruction is in
`docs/reverse-engineering/plat_132/recovered/plat_smac.c`.
